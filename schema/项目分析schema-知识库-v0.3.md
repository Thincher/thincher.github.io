# 项目分析 Schema v2 - 知识库/数据库型（v0.3）

> 根据文章结构写文章，每段必须回答的问题。 **本文档非常重要，修改前务必得到用户的明确同意**

# 要求
- 写入和检索，更新删除。希望是一个mermaid图 + 下方回答各个问题。 
- 我不希望有太多代码，具体文件的位置对我没什么用，我不会去看代码，我只在乎机制。  
- 我希望图里能明确标出来 哪儿用LLM做了什么，有没有用LLM在图中用文字说明。
- 正文中禁止有任何引用文件路径和文件名，要放在最后一章。
- 画mermaid使用 mermaid-visualizer
- 文档中不要有太多专业名词，尽量用中文，有专业名字要加注释。


# 文章结构
## 第一章概述 
- 这个项目解决什么具体问题？设计思路是什么？
- 这个项目的亮点是什么？有什么优势？

## 第二章 写入流程
- 写入后产物的存储是什么样子，给出实际的例子？
- 完整写入流程分几阶段？每阶段产什么？
- 数据怎么进？入口在哪？支持哪些数据源（md/pdf/网页/数据库）？
- Agent 怎么操作这个工具写入，SKILL,MCP每个工具的作用是啥？具体的参数是什么？
- 写入流程中有哪些部分用了LLM，prompt 是啥？
- chunk 怎么切？大小？overlap？语义切分还是规则切分？
- 用embedding了吗，什么时候用的 用的什么模型？

## 第三章 检索流程
- query 到结果分几阶段，每个阶段干了什么？产出了什么？
- 检索流程中有哪些部分用了LLM，prompt 是啥？
- 召回策略用了哪些？每个策略的作用是什么？参数怎么选？(向量召回，BM25召回，索引，重排，图召回,top-k / MMR / hybrid / HyDE / RAG-Fusion...)
- 检索结果怎么拼到 LLM prompt里，给实际拼接好的prompt例子？
- Agent 怎么操作这个工具检索，SKILL,MCP每个工具的作用是啥？具体的参数是什么？

## 第四章 更新与删除

- 更新的整体流程是怎样的？
- 更新的触发条件是啥？更新会更新那些存储介质？
- Agent 怎么操作这个工具更新删除，SKILL,MCP每个工具的作用是啥？具体的参数是什么？

## 第五章 存储结构
- 用了哪几种存储（向量库/关系型/KV/图/文件/对象存储...）？各存什么？存储的数据结构是什么？有什么用处？
- 存储之间的数据流怎么走？


## 参考文件及作用
- 每个章节分别引用了哪些文件，哪些文章。

# 门禁
> 写完后自我检查一下门禁，通过后才能交付给用户
- 是否能回答所有的问题？每个问题每个小问？
- 文章中的内容禁止有编造的内容，需要有实际代码&文章的支持。
- 必须遵守要求的所有内容。